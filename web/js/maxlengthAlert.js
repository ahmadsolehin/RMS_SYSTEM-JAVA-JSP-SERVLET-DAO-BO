//$('').keyup(function () {
//           var maxLength = 50;
//           var text = $(this).val();
//           var textLength = text.length;
//           if (textLength > maxLength) {
//               $(this).val(text.substring(0, (maxLength)));
//               $('#companynamealert').html("Sorry, you only " + maxLength + " characters are allowed");
//           }
//           else {
//               //alert("Required Min. 500 characters");
//           }
//       });
       
//       $('#regno','#companyname').keyup(function () {
//           var maxLength = 30;
//           var text = $(this).val();
//           var textLength = text.length;
//           if (textLength > maxLength) {
//               $(this).val(text.substring(0, (maxLength)));
//               alert("Sorry, you only " + maxLength + " characters are allowed");
//           }
//           else {
//               //alert("Required Min. 500 characters");
//           }
//       });